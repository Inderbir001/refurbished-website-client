"use client";
import Link from "next/link";
import { useState } from "react";
export default function ForgotPassword() { const [message, setMessage] = useState(""); const [url, setUrl] = useState(""); async function submit(form: FormData) { const response = await fetch("/api/auth/password-reset/request", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ email: form.get("email") }) }); const result = await response.json(); setMessage(response.ok ? "If the account exists, a reset notification has been queued." : result.error); setUrl(result.data?.developmentResetUrl ?? ""); } return <section className="auth-page"><form onSubmit={(event) => { event.preventDefault(); void submit(new FormData(event.currentTarget)); }}><p className="eyebrow">ACCOUNT RECOVERY</p><h1>Reset password</h1><label>Email used on your account<input name="email" type="email" required /></label><small className="consent-note">Signed up with only a phone number? Contact support and we will reset your password.</small><button className="primary-button">Request reset</button>{message && <p>{message}</p>}{url && <Link className="text-link" href={url}>Open local-development reset link</Link>}</form></section>; }
