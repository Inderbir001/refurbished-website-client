"use client";
import { useSearchParams } from "next/navigation";
import { Suspense, useState } from "react";
function ResetPasswordForm() { const token = useSearchParams().get("token") ?? ""; const [message, setMessage] = useState(""); async function submit(form: FormData) { const response = await fetch("/api/auth/password-reset/confirm", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ token, password: form.get("password") }) }); const result = await response.json(); setMessage(response.ok ? "Password reset. You can now sign in." : result.error); } return <section className="auth-page"><form onSubmit={(event) => { event.preventDefault(); void submit(new FormData(event.currentTarget)); }}><p className="eyebrow">CHOOSE A NEW PASSWORD</p><h1>New password</h1><label>Password<input name="password" type="password" minLength={8} required /></label><button className="primary-button">Save password</button>{message && <p>{message}</p>}</form></section>; }
export default function ResetPassword() { return <Suspense fallback={<section className="auth-page"><p>Loading reset form…</p></section>}><ResetPasswordForm /></Suspense>; }
