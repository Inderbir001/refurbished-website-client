"use client";
import { useRef, useState } from "react";
type Option = { id: string; name: string };
export function NewProductForm({ storeId, categories, brands }: { storeId: string; categories: Option[]; brands: Option[] }) { const [message, setMessage] = useState(""); const formRef = useRef<HTMLFormElement>(null); async function save(form: FormData) { const value = Object.fromEntries(form); const response = await fetch("/api/admin/products", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ ...value, storeId, basePrice: Math.round(Number(value.basePrice) * 100), salePrice: value.salePrice ? Math.round(Number(value.salePrice) * 100) : undefined, stock: Number(value.stock), warrantyMonths: Number(value.warrantyMonths) }) }); const data = await response.json(); setMessage(response.ok ? `Saved ${data.data.name}. It is now live in the storefront.` : data.error ?? "Could not save product."); if (response.ok) formRef.current?.reset(); }
return <form ref={formRef} action={save} className="product-form"><label>Product name<input name="name" required minLength={3} /></label><label>SEO slug<input name="slug" required pattern="[a-z0-9-]+" placeholder="apple-iphone-13-128gb" /></label><label>Description<textarea name="description" required minLength={20} /></label><label>SKU<input name="sku" required /></label><div className="form-columns"><label>Price (₹)<input name="basePrice" type="number" min="1" required /></label><label>Sale price (₹)<input name="salePrice" type="number" min="1" /></label><label>Opening stock<input name="stock" type="number" min="0" required /></label></div><div className="form-columns"><label>Condition<select name="condition"><option value="NEW">New</option><option value="REFURBISHED">Refurbished</option><option value="USED">Used</option></select></label><label>Category<select name="categoryId"><option value="">Uncategorised</option>{categories.map((item) => <option value={item.id} key={item.id}>{item.name}</option>)}</select></label><label>Brand<select name="brandId"><option value="">Unbranded</option>{brands.map((item) => <option value={item.id} key={item.id}>{item.name}</option>)}</select></label></div><div className="form-columns"><label>Variant title<input name="variantTitle" defaultValue="Standard" required /></label><label>Warranty (months)<input name="warrantyMonths" type="number" min="0" defaultValue="6" /></label><label>Image URL<input name="imageUrl" type="url" placeholder="https://…" /></label></div><button className="primary-button">Publish product</button>{message && <p aria-live="polite">{message}</p>}</form> }
