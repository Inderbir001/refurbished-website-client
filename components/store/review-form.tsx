"use client";
import { useState } from "react";
export function ReviewForm({ productId }: { productId: string }) { const [message, setMessage] = useState(""); async function submit(form: FormData) { const response = await fetch("/api/account/reviews", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ productId, rating: Number(form.get("rating")), body: form.get("body") }) }); const result = await response.json(); setMessage(response.ok ? "Review submitted for moderation." : result.error ?? "Review could not be submitted."); } return <form action={submit} className="review-form"><h3>Review your delivered purchase</h3><label>Rating<select name="rating"><option value="5">5 — Excellent</option><option value="4">4 — Good</option><option value="3">3 — Average</option><option value="2">2 — Poor</option><option value="1">1 — Very poor</option></select></label><label>Review<textarea name="body" minLength={10} required/></label><button className="secondary-button">Submit verified-purchase review</button>{message && <small>{message}</small>}</form>; }
