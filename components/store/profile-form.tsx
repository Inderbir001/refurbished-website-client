"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function ProfileForm({ name, phone }: { name: string; phone: string }) { const router = useRouter(); const [message, setMessage] = useState(""); async function save(form: FormData) { const response = await fetch("/api/account/profile", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify(Object.fromEntries(form)) }); const result = await response.json(); setMessage(response.ok ? "Profile saved." : result.error ?? "Could not save profile."); if (response.ok) router.refresh(); } async function logout() { await fetch("/api/auth/logout", { method: "POST" }); router.push("/"); router.refresh(); } return <div className="profile-grid"><form action={save} className="product-form"><h2>Profile</h2><label>Name<input name="name" defaultValue={name} required /></label><label>Mobile<input name="phone" defaultValue={phone} pattern="[6-9][0-9]{9}" /></label><h3>Change password</h3><label>Current password<input name="currentPassword" type="password" /></label><label>New password<input name="newPassword" type="password" minLength={8} /></label><button className="primary-button">Save profile</button>{message && <p>{message}</p>}</form><div><button className="secondary-button" onClick={logout}>Sign out</button></div></div>; }
