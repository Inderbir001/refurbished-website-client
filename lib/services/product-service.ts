import { AppError } from "../http";
export function assertValidProductPricing(input: { basePrice: number; salePrice?: number | null; costPrice?: number | null }) { if (!Number.isInteger(input.basePrice) || input.basePrice <= 0) throw new AppError(400, "Regular price must be a positive amount."); if (input.salePrice != null && (!Number.isInteger(input.salePrice) || input.salePrice <= 0 || input.salePrice >= input.basePrice)) throw new AppError(400, "Sale price must be positive and lower than the regular price."); if (input.costPrice != null && (!Number.isInteger(input.costPrice) || input.costPrice <= 0)) throw new AppError(400, "Cost price must be a positive amount."); }
export function normalizeProductSlug(value: string) { return value.trim().toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, ""); }
